class UserLocation {
  final double latitude;
  final double longitude;
  final double altitude;
  final double speed;
  final double head;
  UserLocation({this.head, this.altitude, this.speed, this.latitude, this.longitude});
}